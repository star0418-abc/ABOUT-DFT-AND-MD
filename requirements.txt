# Python dependencies for gel_packmol
# Install with: pip install -r requirements.txt

# YAML configuration file parsing
PyYAML>=6.0

# Note: No other external dependencies required.
# The script uses only standard library modules:
# - argparse, json, math, os, sys, pathlib, typing

